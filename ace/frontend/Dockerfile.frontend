FROM node:20-alpine

WORKDIR /app

# Copy package.json + lock
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Build Vite frontend
RUN npm run build

# Install static file server
RUN npm install -g serve

EXPOSE 3000
CMD ["serve", "-s", "dist", "-l", "3000"]