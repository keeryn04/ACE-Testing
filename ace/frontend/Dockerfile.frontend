# Use Node 20
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package.json and package-lock.json (inside frontend context)
COPY package*.json ./

# Install dependencies
RUN npm ci

# Expose Vite port
EXPOSE 5173

# Start dev server on all interfaces (host 0.0.0.0)
CMD ["npm", "run", "dev", "--", "--host"]